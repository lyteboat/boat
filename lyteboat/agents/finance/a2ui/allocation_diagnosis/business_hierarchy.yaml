# business_hierarchy.yaml — allocation_diagnosis: one shape; the blocked and two-bucket variants are data-driven hides

default: allocation_diagnosis

hierarchies:
  allocation_diagnosis:
    root: root-container
    ui_ids:
      - title-text
      - summary-text
      - grid
      - blocked-text
      - caveat-text
      - coverage-text
