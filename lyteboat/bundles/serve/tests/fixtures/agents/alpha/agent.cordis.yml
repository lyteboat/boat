# A persona, and a frame decorator that tags every frame answering this agent.
- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    prefix: >-
      You are SERVE-ALPHA, a test agent powered by the {{model}} model.
    suffix: Your working directory is {{cwd}}.

- id: frame-tag
  name: './frame-tag.mjs'
