{
  "name": "@lyteboat/business-base",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "description": "lyteboat's business base over dsh-base and @lyteboat/host: a business agent gets only the capabilities its own composition declares, so the coding tools and the services behind them, the sandbox and approval rows, the host's skill roots, and the rows that leak the machine's configuration into model requests are off in every business mode (headless, serve, eval)",
  "type": "module",
  "exports": {
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "files": [
    "cordis.patch.yml"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "dependencies": {
    "@deepseek-ai/dsh-fs-local": "catalog:dsh"
  }
}
