{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "lib"
  },
  "include": ["src"],
  // The pages type-check on their own (tsconfig.client.json: DOM and React JSX, no Node);
  // scripts/dist/build-studio-web.ts bundles them into lib/web.
  "exclude": ["src/client"]
}
